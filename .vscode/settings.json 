{
  "name": "multi-language-code-converter",
  "displayName": "Multi-Language Code Converter",
  "description": "Convert code between popular programming languages in VS Code using OpenAI and Docker",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.80.0"
  },
  "activationEvents": [
    "onCommand:multiLangCodeConverter.convert"
  ],
  "main": "./src/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "multiLangCodeConverter.convert",
        "title": "Convert Code"
      }
    ],
    "configuration": {
      "type": "object",
      "title": "Multi Language Converter Settings",
      "properties": {
        "multiLangCodeConverter.openAIKey": {
          "type": "string",
          "default": "",
          "description": "OpenAI API key for code conversion."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "echo 'Nothing to compile'",
    "test": "echo \"No tests yet\""
  },
  "dependencies": {
    "axios": "^1.4.0"
  }
}